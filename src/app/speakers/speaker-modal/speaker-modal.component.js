import './speaker-modal.component.scss';
import template from './speaker-modal.component.html';

export const SpeakerModalComponent = {
    bindings: {
        speaker: "<"
    },
    template: template
}