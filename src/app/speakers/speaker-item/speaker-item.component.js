import "./speaker-item.component.scss";
import template from "./speaker-item.component.html";

export const SpeakerItemComponent = {
    bindings: {
        speaker: "<"
    },
    template: template
}